import { PercentScalePipe } from './percent-scale.pipe';

describe('PercentScalePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentScalePipe();
    expect(pipe).toBeTruthy();
  });
});
