import { LimitOverviewPipe } from './limit-overview.pipe';

describe('LimitOverviewPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitOverviewPipe();
    expect(pipe).toBeTruthy();
  });
});
